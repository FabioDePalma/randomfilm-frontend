
FROM nginx:alpine

# Copia i file build dalla cartella local-frontend-build
COPY local-frontend-build/ /usr/share/nginx/html/

# Copia la configurazione nginx per SPA
COPY nginx.conf /etc/nginx/nginx.conf

# Esponi la porta 80
EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]